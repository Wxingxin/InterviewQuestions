
## 🧱 1️⃣ 原型链继承（最基础）

### ✅ 概念

子类的原型指向父类的实例，让子类实例可以访问父类原型上的方法。

### 📘 代码示例

```js
function Parent() {
  this.name = "Parent";
  this.colors = ["red", "blue"];
}
Parent.prototype.sayHi = function() {
  console.log("Hi from Parent");
};

function Child() {}
Child.prototype = new Parent(); // 关键：原型链继承
Child.prototype.constructor = Child;

const c1 = new Child();
const c2 = new Child();

c1.colors.push("green");
console.log(c2.colors); // ["red", "blue", "green"] ❌ 被共享
c1.sayHi(); // Hi from Parent
```

### ⚠️ 缺点

* 引用类型（如数组）会被所有子实例共享。
* 无法向父类构造函数传参。

---

## 🧱 2️⃣ 借用构造函数继承（构造函数继承）

### ✅ 概念

在子类构造函数内部调用父类构造函数，实现“拷贝式继承”。

### 📘 代码示例

```js
function Parent(name) {
  this.name = name;
  this.colors = ["red", "blue"];
}

function Child(name, age) {
  Parent.call(this, name); // 借用父构造函数
  this.age = age;
}

const c1 = new Child("Alice", 18);
const c2 = new Child("Bob", 20);

c1.colors.push("green");
console.log(c2.colors); // ["red", "blue"] ✅ 不共享
console.log(c1.name); // Alice
```

### ⚠️ 缺点

* 只能继承实例属性，**不能继承父类原型上的方法**。

---

## 🧱 3️⃣ 组合继承（经典继承方式）

### ✅ 概念

把“原型链继承 + 借用构造函数继承”结合在一起，既能继承属性，又能继承方法。

### 📘 代码示例

```js
function Parent(name) {
  this.name = name;
  this.colors = ["red", "blue"];
}
Parent.prototype.sayHi = function() {
  console.log("Hi, I'm " + this.name);
};

function Child(name, age) {
  Parent.call(this, name); // 继承属性
  this.age = age;
}
Child.prototype = new Parent(); // 继承方法
Child.prototype.constructor = Child;

const c1 = new Child("Alice", 18);
c1.sayHi(); // Hi, I'm Alice
c1.colors.push("green");
console.log(c1.colors); // ["red", "blue", "green"]

const c2 = new Child("Bob", 20);
console.log(c2.colors); // ["red", "blue"]
```

### ⚙️ 优点

* 继承了父类实例属性（独立）
* 继承了父类原型方法（共享）

### ⚠️ 缺点

* 调用了两次父类构造函数（性能浪费一点）

---

## 🧱 4️⃣ 原型式继承（Object.create 的原理）

### ✅ 概念

直接通过一个对象创建另一个对象（类似浅拷贝）。
是 `Object.create()` 的原理。

### 📘 代码示例

```js
const parent = {
  name: "Parent",
  colors: ["red", "blue"],
  sayHi() {
    console.log("Hi from Parent");
  }
};

const child = Object.create(parent); // 创建一个以 parent 为原型的对象
child.name = "Child";
child.colors.push("green");

console.log(child.colors); // ["red", "blue", "green"]
console.log(parent.colors); // ["red", "blue", "green"] ❌ 引用被共享
child.sayHi(); // Hi from Parent
```

### ⚠️ 缺点

* 引用类型依然共享。
* 无法传参。

---

## 🧱 5️⃣ 寄生式继承

### ✅ 概念

在“原型式继承”的基础上，**封装一个函数**来增强对象。

### 📘 代码示例

```js
function createAnother(original) {
  const clone = Object.create(original); // 原型式继承
  clone.sayHi = function() { // 增强对象
    console.log("Hi, I'm a new object");
  };
  return clone;
}

const parent = { name: "Parent" };
const child = createAnother(parent);

child.sayHi(); // Hi, I'm a new object
```

### ⚠️ 缺点

* 依然无法复用方法（每个对象都有自己的方法副本）。

---

## 🧱 6️⃣ 寄生组合继承（最完美方案 ✨）

### ✅ 概念

结合“借用构造函数继承”和“原型式继承”，
**避免调用父构造函数两次**。

### 📘 代码示例

```js
function Parent(name) {
  this.name = name;
  this.colors = ["red", "blue"];
}
Parent.prototype.sayHi = function() {
  console.log("Hi, I'm " + this.name);
};

function Child(name, age) {
  Parent.call(this, name); // 继承属性
  this.age = age;
}

// 核心：不直接 new Parent()
Child.prototype = Object.create(Parent.prototype); // 继承方法
Child.prototype.constructor = Child;

const c1 = new Child("Alice", 18);
c1.sayHi(); // Hi, I'm Alice
```

### ✅ 优点

* 不会共享引用类型；
* 不会重复调用父构造函数；
* 方法复用、结构清晰；
* 是 **ES6 class extends** 的底层原理！

---

## 🧭 学习建议顺序（总结）

| 阶段  | 继承方式     | 难度   | 是否常用 | 学习目标                 |
| --- | -------- | ---- | ---- | -------------------- |
| 1️⃣ | 原型链继承    | ⭐    | ❌    | 理解原型查找机制             |
| 2️⃣ | 借用构造函数继承 | ⭐⭐   | ❌    | 理解属性继承               |
| 3️⃣ | 组合继承     | ⭐⭐⭐  | ✅    | 理解属性+方法继承            |
| 4️⃣ | 原型式继承    | ⭐⭐   | ❌    | 理解 `Object.create()` |
| 5️⃣ | 寄生式继承    | ⭐⭐⭐  | ❌    | 理解对象增强               |
| 6️⃣ | 寄生组合继承   | ⭐⭐⭐⭐ | ✅    | 理解最优继承模式（ES6 原理）     |

---

是否希望我帮你画一张
🧭《6 种 JS 继承方式对比图 + 优缺点表 + 查找关系图》
（用箭头展示原型链结构和继承关系）？
那张图会让你一下子明白它们的联系。
