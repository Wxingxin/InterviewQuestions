
---

## 🔍 三、每个部分详细解释 + 示例

### 1️⃣ Entry（入口）

告诉 Webpack 从哪个文件开始构建依赖图。

```js
module.exports = {
  entry: './src/index.js',
};
```

✅ Webpack 会从这里开始，递归分析项目依赖，打包所有模块。

---

### 2️⃣ Output（输出）

告诉 Webpack 打包结果要输出到哪里、叫什么。

```js
module.exports = {
  output: {
    path: __dirname + '/dist',
    filename: 'bundle.js',
  },
};
```

📦 打包完成后，生成 `dist/bundle.js` 文件。

---

### 3️⃣ Loader（加载器）

Webpack 只认识 JavaScript，但我们写的项目中常有：

* CSS
* 图片
* TypeScript
* Vue 文件
* React JSX

👉 Loader 就是把这些非 JS 文件“翻译”为 JS 模块的工具。

```js
module.exports = {
  module: {
    rules: [
      { test: /\.css$/, use: ['style-loader', 'css-loader'] },
      { test: /\.jsx?$/, use: 'babel-loader', exclude: /node_modules/ },
    ],
  },
};
```

📘 例子：

* `css-loader` 让你能 `import './style.css'`
* `babel-loader` 让你能写 ES6 / JSX 代码

---

### 4️⃣ Plugins（插件）

Plugins 用于“增强 Webpack 的功能”，例如：

* 压缩 JS
* 自动生成 HTML
* 清空旧文件
* 定义环境变量

```js
const HtmlWebpackPlugin = require('html-webpack-plugin');
const { CleanWebpackPlugin } = require('clean-webpack-plugin');

module.exports = {
  plugins: [
    new CleanWebpackPlugin(), // 清空 dist 目录
    new HtmlWebpackPlugin({ template: './src/index.html' }), // 生成 HTML 并自动引入 JS
  ],
};
```

---

### 5️⃣ Mode（模式）

Mode 用来告诉 Webpack 你当前是开发环境还是生产环境。

```js
module.exports = {
  mode: 'development', // 或 'production'
};
```

| 模式            | 特点                      |
| ------------- | ----------------------- |
| `development` | 不压缩代码、带 source map、便于调试 |
| `production`  | 自动压缩、优化性能、去除多余代码        |

---

示例：

```js
devServer: {
  static: './dist',
  hot: true,
  open: true
},
resolve: {
  extensions: ['.js', '.jsx', '.json'],
  alias: { '@': './src' }
}
```

---



---

## 🎯 八、总结表格

| 组成部分   | 关键词  | 主要作用                |
| ------ | ---- | ------------------- |
| Entry  | 入口   | 告诉 Webpack 从哪里开始打包  |
| Output | 输出   | 告诉 Webpack 打包后放到哪   |
| Loader | 翻译器  | 让 Webpack 能识别各种类型文件 |
| Plugin | 插件系统 | 扩展功能，如压缩、生成HTML等    |
| Mode   | 环境模式 | 控制打包优化策略            |

---

是否希望我接着帮你画一个 **Webpack 打包流程图（从入口到输出）**？
