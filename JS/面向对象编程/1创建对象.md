# 💯💯💯 基础

### ✅ 1. **对象字面量创建法（最常用、最简单）**

> 直接使用 `{}` 创建对象。

```js
const person = {
  name: "Alice",
  age: 25,
  greet: function () {
    console.log(`Hi, I'm ${this.name}`);
  },
};

person.greet(); // 输出：Hi, I'm Alice
```

📘 优点：最简单、最常用。
📘 缺点：无法复用（每次都要重新写结构）。

---

### ✅ 2. **使用 `new Object()` 构造函数**

> 等价于对象字面量，属于早期写法。

```js
const person = new Object();
person.name = "Bob";
person.age = 30;
person.sayHi = function () {
  console.log(`Hi, I'm ${this.name}`);
};

person.sayHi(); // Hi, I'm Bob
```

📘 优点：早期兼容性好。
📘 缺点：冗长，不推荐新项目中使用。

---

### ✅ 3. **使用工厂函数创建对象（函数创建）**

> 把“创建对象的过程”封装进函数中，复用性强。

```js
function createPerson(name, age) {
  const obj = {};
  obj.name = name;
  obj.age = age;
  obj.sayHi = function () {
    console.log(`Hi, I'm ${this.name}`);
  };
  return obj;
}

const p1 = createPerson("Tom", 20);
const p2 = createPerson("Jerry", 22);

p1.sayHi(); // Hi, I'm Tom
p2.sayHi(); // Hi, I'm Jerry
```

📘 优点：可复用。
📘 缺点：**无法识别对象类型**（`p1.constructor` 是 `Object`），不是真正的“类实例”。

---

### ✅ 4. **使用构造函数创建对象**

> 使用 `this` 和 `new` 关键字，模仿面向对象。

```js
function Person(name, age) {
  this.name = name;
  this.age = age;
  this.sayHi = function () {
    console.log(`Hi, I'm ${this.name}`);
  };
}

const p1 = new Person("Lily", 25);
const p2 = new Person("Lucy", 26);

p1.sayHi(); // Hi, I'm Lily
console.log(p1 instanceof Person); // true
```

📘 原理：`new` 会创建一个空对象 → 绑定 `this` → 返回对象。
📘 缺点：`sayHi` 方法在每个实例上都会重复创建（浪费内存）。

# 💯💯💯 other

### ✅ 7. **使用 `Object.create()`**

> 以一个对象为原型创建另一个对象。

```js
const personProto = {
  greet() {
    console.log(`Hi, I'm ${this.name}`);
  },
};

const person = Object.create(personProto);
person.name = "Henry";
person.greet(); // Hi, I'm Henry
```

📘 优点：可以精确控制原型。
📘 缺点：需要手动设置属性，不如 class 直观。

---

### ✅ 8. **使用 ES6 的 `Object.assign()`**

> 用于复制多个对象属性到一个目标对象。

```js
const base = { name: "Ivan" };
const extra = { age: 30, city: "Beijing" };

const person = Object.assign({}, base, extra);
console.log(person); // { name: 'Ivan', age: 30, city: 'Beijing' }
```

📘 用法：常用于对象合并、浅拷贝。
📘 缺点：不是“类”的创建方式，只是合并对象。
# 💯💯💯 obj Function

## 对象冻结/锁定/扩展控制

* `Object.preventExtensions(obj)`：禁止添加新属性。
* `Object.seal(obj)`：禁止添加/删除属性，值可改。
* `Object.freeze(obj)`：完全冻结，属性不可增删改。

---



* **创建与原型**

  * `Object.create(proto)`
  * `Object.getPrototypeOf(obj)`
  * `Object.setPrototypeOf(obj, proto)`

* **属性操作**

  * `Object.defineProperty(obj, key, descriptor)`
  * `Object.defineProperties(obj, descriptors)`
  * `Object.getOwnPropertyDescriptor(obj, key)`
  * `Object.getOwnPropertyDescriptors(obj)`

* **遍历/获取**

  * `Object.keys(obj)`
  * `Object.values(obj)`
  * `Object.entries(obj)`

* **合并/拷贝**

  * `Object.assign(target, ...sources)`

* **属性状态**

  * `Object.isExtensible(obj)`
  * `Object.isSealed(obj)`
  * `Object.isFrozen(obj)`

---